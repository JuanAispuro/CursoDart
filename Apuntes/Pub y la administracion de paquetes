    -- Pubspec.yaml --
Administrar las librerias y dependencias del programa.
Se puede configurar otros programas.

yaml - Gestiona las dependencias y librerias de nuestro proyecto.
pub get - ver las dependencias.
pub upgrade - actualizar las dependencias
usar primero dart antes de pub.

Al guardar el archivo la mayoria se actualiza automaticamente.

Dependencias 

-- Pub --
Donde estan las librerias pub.dev

-- Adicionado y utilizando la libreria --
import 'package:random_string/random_string.dart';
import 'dart:math' show Random;

void main() {
  print(randomBetween(7, 100000)); //Numero random entre esos numeros
  print(randomNumeric(6)); //Numero random con 6 digitos
  print(randomString(10)); //Caracter random entre 10 digitos
  
}

-- Librerias de desarrollo dev dependencies --
pub.dev

-- adicionando scripts Shortcuts --
usar libreria derri implementando scripts en pubspec.yaml
activar dentro del ordenador (pub global activate derry).
usar siempre dart derry


-- Consejos a la hora de utilizar librerias --
Revisar las  dependencias que ocupa cada libreria.
Tambien revisar la seccion de errores y issues que existan.




